Run as:
 dltest -d <library_to_check> ..

dltest will try to dlopen() all libs it gets on command line and
will print errors when something fails.

when run with -d, it also deletes libraries which fail to open

